meta {
  name: liteworkflow MVP
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{adamToken}}
}

docs {
  Runnable M14 MVP collection. Select the Local environment and run requests in
  sequence. Runtime IDs and bearer tokens are ephemeral and are never persisted.
}
