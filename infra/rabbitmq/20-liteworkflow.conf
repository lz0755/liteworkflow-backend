# Limit the seeded application user to its own vhost in the management UI.
# Messaging permissions remain configure/write/read on the seeded vhost so
# Spring AMQP can declare exchanges, queues, and bindings during development.
default_user_tags.administrator = false
default_user_tags.management = true
